import { Outlet } from "react-router-dom";

const Consultations = () => {
    return (
        <div>
            <h1>Consultations</h1>
            <Outlet />
        </div>
    );
};

export default Consultations;
